---
name: add-frontend-module
description: Add a tree-shakeable module to the MainMoney JS/TS frontend SDK
---

# Add a frontend module

1. Add a workspace under `packages/` with TypeScript `src/index.ts` and `.d.ts` emit.
2. Keep the core framework-agnostic.
3. README must state that a backend SDK is required.
